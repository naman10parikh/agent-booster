# tools/

Repo-local tools for `agent-booster` (scaffold — not yet populated).

Reserved for auxiliary scripts and utilities that support the CLI but are not part of the shipped
`dist/` binary. Harness utilities (memory search/compress, budget manager) live in
[`scripts/`](../scripts/).

See the harness component map in [CLAUDE.md](../CLAUDE.md) and the hub
[`brain/MOC - agent-booster.md`](../brain/MOC%20-%20agent-booster.md).
